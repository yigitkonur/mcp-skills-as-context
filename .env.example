# GitHub Personal Access Tokens (comma-separated for round-robin rate limiting)
GITHUB_TOKENS=

# Single GitHub token (fallback if GITHUB_TOKENS not set)
GITHUB_TOKEN=

# Server port (default: 3000)
PORT=3000
